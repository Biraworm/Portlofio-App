{
  "name": "portfolio-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd apps/frontend && npm run dev",
    "dev:backend": "cd apps/backend && npm run start:dev",
    "start": "./scripts/setup-and-start.sh",
    "start:simple": "node scripts/check-and-start-backend.js && cd apps/frontend && npm run dev",
    "stop": "./scripts/stop.sh",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd apps/frontend && npm run build",
    "build:backend": "cd apps/backend && npm run build",
    "install:all": "npm install && cd apps/frontend && npm install && cd ../backend && npm install",
    "vercel-build": "cd apps/frontend && npm install --legacy-peer-deps && npm run build"
  },
  "dependencies": {
    "next": "14.2.5",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
